# ZagEditor portable settings (edit before use)
# dict_path is relative to the package root when started via Run-ZagEditor.bat
dict_path=data\EXPORT.INI
recursive=true
backup=false
# Semicolon-separated watch/input directories. Replace with real paths.
input_dirs=D:\in
